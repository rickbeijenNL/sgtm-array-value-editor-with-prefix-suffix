# sgtm-array-value-editor-with-prefix-suffix
SGTM variable template. Optionally add a prefix or suffix to array values. When enabled, the transform value wraps the original. If both are off, the original value is fully replaced.
